{"files":{"dictutil.py":"\"\"\"dictutil — non-mutating deep merge.\"\"\"\nimport copy\n\n\ndef merge(a: dict, b: dict) -> dict:\n    \"\"\"Deep-merge dicts a and b into a NEW dict without mutating inputs.\n\n    Values from b win on conflicts; nested dicts merge recursively.\n    Non-dict values (including lists) are replaced by b's value.\n    Inputs are never mutated; the result shares no nested objects with them.\n    \"\"\"\n    result = {}\n    for key in set(a) | set(b):\n        if key in a and key in b:\n            av, bv = a[key], b[key]\n            if isinstance(av, dict) and isinstance(bv, dict):\n                result[key] = merge(av, bv)\n            else:\n                result[key] = copy.deepcopy(bv)\n        elif key in a:\n            result[key] = copy.deepcopy(a[key])\n        else:\n            result[key] = copy.deepcopy(b[key])\n    return result","test_dictutil.py":"\"\"\"Tests for dictutil.merge (run: python test_dictutil.py).\"\"\"\nimport copy\nfrom dictutil import merge\n\n\ndef test_merge_basic():\n    assert merge({\"a\": 1}, {\"b\": 2}) == {\"a\": 1, \"b\": 2}\n\n\ndef test_merge_conflict_b_wins():\n    assert merge({\"a\": 1}, {\"a\": 2}) == {\"a\": 2}\n\n\ndef test_merge_nested_dicts_recursively():\n    a = {\"x\": {\"p\": 1, \"q\": 2}}\n    b = {\"x\": {\"q\": 3, \"r\": 4}}\n    assert merge(a, b) == {\"x\": {\"p\": 1, \"q\": 3, \"r\": 4}}\n\n\ndef test_no_mutation_of_inputs():\n    a = {\"x\": {\"p\": 1}}\n    b = {\"y\": 2}\n    a0, b0 = copy.deepcopy(a), copy.deepcopy(b)\n    merged = merge(a, b)\n    assert merged is not a and merged is not b\n    assert merged[\"x\"] is not a[\"x\"]\n    # mutating the result must not affect the inputs either\n    merged[\"x\"][\"p\"] = 999\n    assert a == a0 and b == b0\n\n\ndef test_non_dict_value_replaced():\n    assert merge({\"a\": [1, 2]}, {\"a\": {\"b\": 1}}) == {\"a\": {\"b\": 1}}\n    assert merge({\"a\": {\"b\": 1}}, {\"a\": [1]}) == {\"a\": [1]}\n\n\nif __name__ == \"__main__\":\n    fns = [v for k, v in sorted(globals().items()) if k.startswith(\"test_\")]\n    for fn in fns:\n        fn()\n        print(\"PASS\", fn.__name__)\n    print(len(fns), \"tests passed\")"}}