{"files":{"divmod2.py":"\"\"\"divmod2 — integer division with explicit remainder check.\"\"\"\n\n\ndef div_check(a, b):\n    \"\"\"Return [q, r] where a == q * b + r, 0 <= r < |b|.\n\n    Raises ZeroDivisionError when b == 0.\n    \"\"\"\n    if b == 0:\n        raise ZeroDivisionError(\"integer division or modulo by zero\")\n    q = a // b\n    r = a % b\n    assert a == q * b + r and (r >= 0 if b > 0 else r > -abs(b)), \"invariant violated\"\n    return [q, r]","test_divmod2.py":"import pytest\nfrom divmod2 import div_check\n\n\ndef test_basic():\n    assert div_check(7, 3) == [2, 1]\n\n\ndef test_exact_division():\n    assert div_check(6, 3) == [2, 0]\n\n\ndef test_negative_a():\n    assert div_check(-7, 3) == [-3, 2]  # Python floor-division semantics\n\n\ndef test_negative_b():\n    assert div_check(7, -3) == [-3, -2]  # floor semantics with negative divisor\n\n\ndef test_both_negative():\n    assert div_check(-7, -3) == [2, -1]\n\n\ndef test_zero():\n    assert div_check(0, 5) == [0, 0]\n\n\ndef test_zero_divisor():\n    with pytest.raises(ZeroDivisionError):\n        div_check(7, 0)\n\n\ndef test_invariant_holds():\n    for a in range(-20, 21):\n        for b in range(-7, 8):\n            if b != 0:\n                q, r = div_check(a, b)\n                assert a == q * b + r and abs(r) < abs(b)\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(pytest.main([__file__]))"}}