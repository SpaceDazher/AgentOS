{"files":{"clamp.py":"\"\"\"Clamp a value into a closed interval [lo, hi].\"\"\"\n\n\ndef clamp(v, lo, hi):\n    \"\"\"Return v clamped to the range [lo, hi].\n\n    >>> clamp(5, 0, 10)\n    5\n    >>> clamp(-3, 0, 10)\n    0\n    >>> clamp(15, 0, 10)\n    10\n    \"\"\"\n    if lo > hi:\n        raise ValueError(\"lo must be <= hi\")\n    return max(lo, min(hi, v))","test_clamp.py":"import pytest\n\nfrom clamp import clamp\n\n\ndef test_value_inside_range():\n    assert clamp(5, 0, 10) == 5\n\n\ndef test_value_below_range():\n    assert clamp(-3, 0, 10) == 0\n\n\ndef test_value_above_range():\n    assert clamp(15, 0, 10) == 10\n\n\ndef test_boundaries_inclusive():\n    assert clamp(0, 0, 10) == 0\n    assert clamp(10, 0, 10) == 10\n\n\ndef test_floats():\n    assert clamp(2.5, 1.0, 2.0) == 2.0\n    assert clamp(1.25, 1.0, 2.0) == 1.25\n\n\ndef test_negative_range():\n    assert clamp(-5, -10, -1) == -5\n    assert clamp(-20, -10, -1) == -10\n\n\ndef test_invalid_bounds_raises():\n    with pytest.raises(ValueError):\n        clamp(5, 10, 0)"}}