{"files":{"strutil.py":"def reverse(s: str) -> str:\n    \"\"\"Return the reversed string.\"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"reverse() expects a string\")\n    return s[::-1]","test_strutil.py":"import pytest\n\nfrom strutil import reverse\n\n\n@pytest.mark.parametrize(\n    \"s, expected\",\n    [\n        (\"\", \"\"),\n        (\"a\", \"a\"),\n        (\"abc\", \"cba\"),\n        (\"hello world\", \"dlrow olleh\"),\n        (\"racecar\", \"racecar\"),          # palindrome is stable\n        (\"привет\", \"тевирп\"),            # unicode (Cyrillic)\n        (\"héllo\", \"olléh\"),              # accented unicode\n    ],\n)\ndef test_reverse_basic(s, expected):\n    assert reverse(s) == expected\n\n\ndef test_reverse_returns_new_string():\n    original = \"abc\"\n    result = reverse(original)\n    assert original == \"abc\"          # input unchanged\n    assert result is not original     # new object for non-trivial strings\n    assert isinstance(result, str)\n\n\ndef test_reverse_rejects_non_string():\n    with pytest.raises(TypeError):\n        reverse(123)\n    with pytest.raises(TypeError):\n        reverse(None)\n    with pytest.raises(TypeError):\n        reverse([\"a\", \"b\"])\n\n\ndef test_reverse_is_involution():\n    s = \"AgentOS\"\n    assert reverse(reverse(s)) == s"}}