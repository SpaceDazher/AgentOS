{"files":{"lists.py":"\"\"\"Module: lists.py — exposes maximum(xs) -> number.\"\"\"\n\ndef maximum(xs):\n    \"\"\"Return the maximum element of a non-empty iterable of numbers.\n\n    Raises:\n        ValueError: if xs is empty.\n        TypeError: if xs is not iterable or contains non-comparable items.\n    \"\"\"\n    iterator = iter(xs)\n    try:\n        first = next(iterator)\n    except StopIteration:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    best = first\n    for item in iterator:\n        if item > best:\n            best = item\n    return best","test_lists.py":"import math\n\nimport pytest\n\nfrom lists import maximum\n\n\ndef test_maximum_basic():\n    assert maximum([1, 2, 3]) == 3\n\n\ndef test_maximum_single_element():\n    assert maximum([42]) == 42\n\n\ndef test_maximum_negative_numbers():\n    assert maximum([-5, -1, -3]) == -1\n\n\ndef test_maximum_floats():\n    assert maximum([1.5, 2.7, 0.3]) == 2.7\n\n\ndef test_maximum_mixed_int_float():\n    assert maximum([1, 2.5, 2]) == 2.5\n\n\ndef test_maximum_duplicates():\n    assert maximum([7, 7, 7]) == 7\n\n\ndef test_maximum_empty_raises_value_error():\n    with pytest.raises(ValueError):\n        maximum([])\n\n\ndef test_maximum_accepts_any_iterable():\n    assert maximum(x * 2 for x in range(5)) == 8\n    assert maximum((9, 4)) == 9\n\n\ndef test_maximum_result_is_numeric():\n    result = maximum([1, 10, 3])\n    assert isinstance(result, (int, float))\n    assert not isinstance(result, bool)\n    assert math.isfinite(result)"}}