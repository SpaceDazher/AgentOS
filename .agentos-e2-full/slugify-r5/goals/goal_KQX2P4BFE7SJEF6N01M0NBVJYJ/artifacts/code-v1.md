{"files":{"slug.py":"import re\nimport unicodedata\n\n__all__ = [\"slugify\"]\n\n\ndef slugify(s: str) -> str:\n    \"\"\"Convert a string to a URL-friendly slug.\n\n    'Hello World' -> 'hello-world'\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"slugify() expects a string\")\n\n    # Normalize unicode and strip diacritics (café -> cafe)\n    text = unicodedata.normalize(\"NFKD\", s)\n    text = \"\".join(ch for ch in text if not unicodedata.combining(ch))\n\n    # Lowercase, replace non-alphanumeric runs with hyphens\n    text = re.sub(r\"[^a-z0-9]+\", \"-\", text.lower())\n\n    # Trim leading/trailing hyphens\n    return text.strip(\"-\")","test_slug.py":"import pytest\n\nfrom slug import slugify\n\n\ndef test_hello_world():\n    assert slugify(\"Hello World\") == \"hello-world\"\n\n\ndef test_extra_whitespace_and_punct():\n    assert slugify(\"  Hello,   World!  \") == \"hello-world\"\n\n\ndef test_mixed_case_digits():\n    assert slugify(\"Version 2 Beta\") == \"version-2-beta\"\n\n\ndef test_diacritics():\n    assert slugify(\"Café Crème\") == \"cafe-creme\"\n\n\ndef test_empty():\n    assert slugify(\"\") == \"\"\n\n\ndef test_non_string_raises():\n    with pytest.raises(TypeError):\n        slugify(123)"}}