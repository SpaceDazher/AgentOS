{"files":{"slug.py":"\"\"\"slugify: convert a string to a URL-friendly slug.\"\"\"\n\nimport re\nimport unicodedata\n\n\ndef slugify(s: str) -> str:\n    \"\"\"Return a lowercase, hyphen-separated slug for *s*.\n\n    'Hello World' -> 'hello-world'\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"slugify expects a string\")\n    # Normalize and strip accents (café -> cafe)\n    s = unicodedata.normalize(\"NFKD\", s).encode(\"ascii\", \"ignore\").decode(\"ascii\")\n    s = s.lower()\n    # Replace any run of non-alphanumeric characters with a single hyphen\n    s = re.sub(r\"[^a-z0-9]+\", \"-\", s).strip(\"-\")\n    return s","test_slug.py":"import pytest\n\nfrom slug import slugify\n\n\ndef test_hello_world():\n    assert slugify(\"Hello World\") == \"hello-world\"\n\n\ndef test_extra_whitespace_and_punct():\n    assert slugify(\"  Hello,   World!  \") == \"hello-world\"\n\n\ndef test_mixed_case_digits():\n    assert slugify(\"My Post #42\") == \"my-post-42\"\n\n\ndef test_accents():\n    assert slugify(\"Café Crème\") == \"cafe-creme\"\n\n\ndef test_empty():\n    assert slugify(\"\") == \"\"\n\n\ndef test_non_string_raises():\n    with pytest.raises(TypeError):\n        slugify(123)"}}