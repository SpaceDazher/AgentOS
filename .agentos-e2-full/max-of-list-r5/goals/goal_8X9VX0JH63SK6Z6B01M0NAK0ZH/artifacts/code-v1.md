{"files":{"lists.py":"\"\"\"Maximum-of-list module.\"\"\"\n\n\ndef maximum(xs):\n    \"\"\"Return the largest element of xs.\n\n    Raises:\n        ValueError: if xs is empty.\n    \"\"\"\n    if not xs:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    result = xs[0]\n    for x in xs[1:]:\n        if x > result:\n            result = x\n    return result","test_lists.py":"import pytest\n\nfrom lists import maximum\n\n\ndef test_maximum_basic():\n    assert maximum([1, 2, 3]) == 3\n    assert maximum([5]) == 5\n    assert maximum([-10, -5, -30]) == -5\n\n\ndef test_maximum_unsorted():\n    assert maximum([7, 2, 9, 4]) == 9\n\n\ndef test_maximum_empty_raises():\n    with pytest.raises(ValueError):\n        maximum([])\n\n\ndef test_maximum_floats_and_mix():\n    assert maximum([1.5, 2.5, 0.5]) == 2.5\n    assert maximum([1, 2.5, 2]) == 2.5"}}