{"files":{"divmod2.py":"def _div(a, b):\n    if b == 0:\n        raise ZeroDivisionError(\"division by zero\")\n    q = a // b\n    r = a % b\n    return q, r\n\n\ndef div_check(a, b):\n    \"\"\"Return [q, r] where q = a // b and r = a % b.\"\"\"\n    q, r = _div(a, b)\n    return [q, r]","test_divmod2.py":"import pytest\n\nfrom divmod2 import div_check\n\n\ndef test_basic():\n    assert div_check(10, 3) == [3, 1]\n\n\ndef test_exact():\n    assert div_check(12, 4) == [3, 0]\n\n\ndef test_negative():\n    assert div_check(-7, 2) == [-4, 1]\n\n\ndef test_zero_dividend():\n    assert div_check(0, 5) == [0, 0]\n\n\ndef test_zero_divisor():\n    with pytest.raises(ZeroDivisionError):\n        div_check(1, 0)\n\n\ndef test_roundtrip():\n    for a in range(-20, 21):\n        for b in (-5, -2, -1, 1, 2, 5):\n            q, r = div_check(a, b)\n            assert b * q + r == a\n            assert abs(r) < abs(b) if b else True\n            assert (r > 0) if b < 0 else (r >= 0)"}}