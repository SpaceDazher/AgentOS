{"files":{"lists2.py":"def minimum(xs):\n    \"\"\"Return the minimum element of a non-empty sequence.\n\n    Raises:\n        ValueError: if xs is empty.\n    \"\"\"\n    if not xs:\n        raise ValueError(\"minimum() arg is an empty sequence\")\n    result = xs[0]\n    for x in xs[1:]:\n        if x < result:\n            result = x\n    return result","tests_lists2.py":"import pytest\n\nfrom lists2 import minimum\n\n\ndef test_returns_smallest_element():\n    assert minimum([3, 1, 2]) == 1\n\n\ndef test_single_element():\n    assert minimum([42]) == 42\n\n\ndef test_negative_numbers():\n    assert minimum([-5, -1, -10]) == -10\n\n\ndef test_floats_and_ints_mixed():\n    assert minimum([2, 0.5, 7]) == 0.5\n\n\ndef test_works_on_tuples():\n    assert minimum((9, 4, 6)) == 4\n\n\ndef test_empty_raises_value_error():\n    with pytest.raises(ValueError):\n        minimum([])"}}