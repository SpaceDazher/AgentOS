{"files":{"lists2.py":"\"\"\"Minimum-of-list module.\"\"\"\n\ndef minimum(xs):\n    \"\"\"Return the smallest element of a non-empty iterable of numbers.\n\n    Raises:\n        ValueError: if xs is empty.\n    \"\"\"\n    xs = list(xs)\n    if not xs:\n        raise ValueError(\"minimum() arg is an empty sequence\")\n    m = xs[0]\n    for x in xs[1:]:\n        if x < m:\n            m = x\n    return m","test_lists2.py":"import math\n\nimport pytest\n\nfrom lists2 import minimum\n\n\ndef test_returns_minimum_of_ints():\n    assert minimum([3, 1, 2]) == 1\n\n\ndef test_single_element():\n    assert minimum([42]) == 42\n\n\ndef test_negative_numbers():\n    assert minimum([-5, -1, -10]) == -10\n\n\ndef test_floats():\n    assert minimum([2.5, 0.1, 1.0]) == pytest.approx(0.1)\n\n\ndef test_mixed_int_float():\n    assert minimum([1, 0.5, 2]) == 0.5\n\n\ndef test_duplicates():\n    assert minimum([7, 7, 7]) == 7\n\n\ndef test_empty_raises_value_error():\n    with pytest.raises(ValueError):\n        minimum([])\n\n\ndef test_returns_number_type():\n    result = minimum([1.5, 2.5])\n    assert isinstance(result, (int, float)) and not isinstance(result, bool)\n    assert math.isfinite(result)"}}