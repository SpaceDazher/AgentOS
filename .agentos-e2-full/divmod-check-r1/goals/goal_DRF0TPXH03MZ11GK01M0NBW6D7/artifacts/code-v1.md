{"files":{"divmod2.py":"\"\"\"divmod2: div_check(a, b) -> [q, r]\"\"\"\n\n\ndef div_check(a, b):\n    \"\"\"Return [quotient, remainder] of a divided by b.\n\n    Raises ZeroDivisionError if b == 0.\n    \"\"\"\n    if b == 0:\n        raise ZeroDivisionError(\"division by zero\")\n    q = a // b\n    r = a % b\n    return [q, r]","test_divmod2.py":"import pytest\n\nfrom divmod2 import div_check\n\n\ndef test_basic():\n    assert div_check(10, 3) == [3, 1]\n\n\ndef test_exact():\n    assert div_check(12, 4) == [3, 0]\n\n\ndef test_zero_remainder_negative():\n    assert div_check(-7, 2) == [-4, 1]  # Python floor-division semantics\n\n\ndef test_negative_divisor():\n    assert div_check(7, -2) == [-4, -1]  # Python floor-division semantics\n\n\ndef test_zero_dividend():\n    assert div_check(0, 5) == [0, 0]\n\n\ndef test_zero_divisor_raises():\n    with pytest.raises(ZeroDivisionError):\n        div_check(1, 0)\n\n\ndef test_returns_list():\n    result = div_check(9, 2)\n    assert isinstance(result, list)\n    assert result == [4, 1]"}}