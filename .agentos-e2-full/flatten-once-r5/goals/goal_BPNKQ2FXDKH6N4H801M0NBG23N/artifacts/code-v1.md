{"files":{"flatten.py":"\"\"\"flatten_once: flatten a list of lists by exactly one level.\"\"\"\n\ndef flatten_once(xss):\n    \"\"\"Return a new list with elements of xss's sublists, one level deep.\n\n    Example:\n        flatten_once([[1, 2], [3]]) -> [1, 2, 3]\n    \"\"\"\n    if not isinstance(xss, (list, tuple)):\n        raise TypeError(f\"expected list of lists, got {type(xss).__name__}\")\n    result = []\n    for item in xss:\n        if isinstance(item, (list, tuple)):\n            result.extend(item)\n        else:\n            result.append(item)\n    return result","test_flatten.py":"import pytest\nfrom flatten import flatten_once\n\n\ndef test_basic():\n    assert flatten_once([[1, 2], [3]]) == [1, 2, 3]\n\n\ndef test_empty_outer():\n    assert flatten_once([]) == []\n\n\ndef test_empty_inner():\n    assert flatten_once([[], [1], []]) == [1]\n\n\ndef test_mixed_scalars_and_lists():\n    assert flatten_once([1, [2, 3], 4]) == [1, 2, 3, 4]\n\n\ndef test_does_not_flatten_deeply():\n    assert flatten_once([[1, [2]], [3]]) == [1, [2], 3]\n\n\ndef test_tuples_inner():\n    assert flatten_once([(1, 2), (3,)]) == [1, 2, 3]\n\n\ndef test_returns_new_list():\n    src = [[1, 2]]\n    out = flatten_once(src)\n    assert out is not src[0]\n    src[0].append(99)\n    assert out == [1, 2]\n\n\ndef test_rejects_non_iterable_input():\n    with pytest.raises(TypeError):\n        flatten_once(42)"}}