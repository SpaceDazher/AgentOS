{"files":{"lists.py":"\"\"\"Maximum of a list.\"\"\"\n\ndef maximum(xs):\n    \"\"\"Return the maximum value in xs.\n\n    Raises ValueError if xs is empty.\n    \"\"\"\n    if not xs:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    result = xs[0]\n    for x in xs[1:]:\n        if x > result:\n            result = x\n    return result","test_lists.py":"import pytest\n\nfrom lists import maximum\n\n\ndef test_maximum_basic():\n    assert maximum([1, 2, 3]) == 3\n    assert maximum([3, 2, 1]) == 3\n    assert maximum([5]) == 5\n\n\ndef test_maximum_negative_and_floats():\n    assert maximum([-10, -3, -50]) == -3\n    assert maximum([1.5, 2.7, 0.1]) == 2.7\n\n\ndef test_maximum_duplicates():\n    assert maximum([7, 7, 7]) == 7\n    assert maximum([2, 9, 9, 4]) == 9\n\n\ndef test_maximum_empty_raises():\n    with pytest.raises(ValueError):\n        maximum([])\n\n\ndef test_returns_element_type():\n    xs = [1.0, 2.0]\n    assert maximum(xs) == 2.0 and isinstance(maximum(xs), float)\n    assert maximum(xs) in xs  # returns an actual element, not a copy"}}