{"files":{"flatten.py":"\"\"\"flatten_once: flatten a list of lists exactly one level.\"\"\"\n\n\ndef flatten_once(xss):\n    \"\"\"Return a new list with one level of nesting removed.\n\n    [[1, 2], [3]] -> [1, 2, 3]\n\n    Non-list items at the top level are kept as-is; inner items are not\n    recursed into.\n    \"\"\"\n    result = []\n    for item in xss:\n        if isinstance(item, list):\n            result.extend(item)\n        else:\n            result.append(item)\n    return result","test_flatten.py":"from flatten import flatten_once\n\n\ndef test_basic():\n    assert flatten_once([[1, 2], [3]]) == [1, 2, 3]\n\n\ndef test_empty():\n    assert flatten_once([]) == []\n    assert flatten_once([[], []]) == []\n\n\ndef test_one_level_only():\n    assert flatten_once([[1, [2]], [3]]) == [1, [2], 3]\n\n\ndef test_non_list_items_kept():\n    assert flatten_once([1, [2], \"a\"]) == [1, 2, \"a\"]\n\n\ndef test_input_not_mutated():\n    xss = [[1, 2], [3]]\n    flatten_once(xss)\n    assert xss == [[1, 2], [3]]"}}