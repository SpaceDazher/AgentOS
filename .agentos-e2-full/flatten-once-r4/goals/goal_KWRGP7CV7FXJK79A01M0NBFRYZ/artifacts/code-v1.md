{"files":{"flatten.py":"\"\"\"flatten_once: flatten a list of lists by exactly one level.\"\"\"\n\n\ndef flatten_once(xss):\n    \"\"\"Return a new list with one level of nesting removed.\n\n    >>> flatten_once([[1, 2], [3]])\n    [1, 2, 3]\n    >>> flatten_once([])\n    []\n    >>> flatten_once([[], [1], []])\n    [1]\n    >>> flatten_once([[1, [2]], [3]])\n    [1, [2], 3]\n    \"\"\"\n    if not isinstance(xss, list):\n        raise TypeError(f\"expected list of lists, got {type(xss).__name__}\")\n    result = []\n    for i, item in enumerate(xss):\n        if not isinstance(item, list):\n            raise TypeError(f\"element {i} is not a list: {type(item).__name__}\")\n        result.extend(item)\n    return result\n\n\nif __name__ == \"__main__\":\n    import doctest\n    failures, _ = doctest.testmod()\n    assert flatten_once([[1, 2], [3]]) == [1, 2, 3]\n    assert flatten_once([]) == []\n    assert flatten_once([[], [1], []]) == [1]\n    assert flatten_once([[1, [2]], [3]]) == [1, [2], 3]\n    print(\"all tests passed\" if failures == 0 else \"doctest failures\")"}}