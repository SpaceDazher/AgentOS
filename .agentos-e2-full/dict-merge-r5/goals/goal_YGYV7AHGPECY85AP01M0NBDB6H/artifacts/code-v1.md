{"files":{"dictutil.py":"\"\"\"dictutil: non-mutating deep-ish merge for plain dicts.\"\"\"\n\nfrom copy import deepcopy\n\n\ndef merge(a: dict, b: dict) -> dict:\n    \"\"\"Return a new merged dict from a and b.\n\n    Neither `a` nor `b` (nor any nested dicts inside them) is mutated.\n    Values from `b` override values from `a`; when both values are dicts,\n    they are merged recursively.\n    \"\"\"\n    if not isinstance(a, dict) or not isinstance(b, dict):\n        raise TypeError(\"merge() expects two dict arguments\")\n\n    result = deepcopy(a)\n\n    def _walk(dst: dict, src_b: dict) -> None:\n        for key, bval in src_b.items():\n            if (\n                isinstance(bval, dict)\n                and isinstance(dst.get(key), dict)\n            ):\n                _walk(dst[key], bval)\n            else:\n                dst[key] = deepcopy(bval)\n\n    _walk(result, b)\n    return result","test_dictutil.py":"\"\"\"Tests for dictutil.merge — run with pytest or plain python.\"\"\"\n\nimport sys\n\nfrom dictutil import merge\n\n\ndef test_flat_merge():\n    a = {\"x\": 1, \"y\": 2}\n    b = {\"y\": 3, \"z\": 4}\n    assert merge(a, b) == {\"x\": 1, \"y\": 3, \"z\": 4}\n\n\ndef test_no_mutation_of_inputs():\n    a = {\"x\": 1, \"nested\": {\"p\": 1}}\n    b = {\"nested\": {\"q\": 2}, \"x\": 9}\n    result = merge(a, b)\n    # Inputs unchanged\n    assert a == {\"x\": 1, \"nested\": {\"p\": 1}}\n    assert b == {\"nested\": {\"q\": 2}, \"x\": 9}\n    assert result == {\"x\": 9, \"nested\": {\"p\": 1, \"q\": 2}}\n    # Nested dicts are independent copies\n    result[\"nested\"][\"p\"] = 999\n    assert a[\"nested\"][\"p\"] == 1\n\n\ndef test_nested_merge():\n    a = {\"db\": {\"host\": \"a\", \"port\": 1, \"opts\": {\"ssl\": False}}}\n    b = {\"db\": {\"port\": 2, \"opts\": {\"timeout\": 5}}}\n    assert merge(a, b) == {\n        \"db\": {\"host\": \"a\", \"port\": 2, \"opts\": {\"ssl\": False, \"timeout\": 5}}\n    }\n\n\ndef test_returns_dict():\n    assert isinstance(merge({}, {}), dict)\n\n\ndef test_type_error():\n    try:\n        merge([], {})  # type: ignore[arg-type]\n    except TypeError:\n        pass\n    else:\n        raise AssertionError(\"expected TypeError\")\n\n\nif __name__ == \"__main__\":\n    test_flat_merge()\n    test_no_mutation_of_inputs()\n    test_nested_merge()\n    test_returns_dict()\n    test_type_error()\n    print(\"ALL TESTS PASSED\")\n    sys.exit(0)"}}