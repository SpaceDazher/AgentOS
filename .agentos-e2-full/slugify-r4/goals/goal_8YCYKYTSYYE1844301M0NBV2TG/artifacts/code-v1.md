{"files":{"slug.py":"import re\nimport unicodedata\n\n\ndef slugify(s: str) -> str:\n    \"\"\"Convert a string to a URL-friendly slug.\n\n    'Hello World' -> 'hello-world'\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"slugify() expects a string\")\n    # Normalize unicode and strip accents (e.g. 'café' -> 'cafe')\n    s = unicodedata.normalize(\"NFKD\", s)\n    s = s.encode(\"ascii\", \"ignore\").decode(\"ascii\")\n    s = s.lower()\n    # Replace non-alphanumeric runs with a single hyphen\n    s = re.sub(r\"[^a-z0-9]+\", \"-\", s)\n    return s.strip(\"-\")","test_slug.py":"import pytest\n\nfrom slug import slugify\n\n\ndef test_hello_world():\n    assert slugify(\"Hello World\") == \"hello-world\"\n\n\ndef test_basic_words():\n    assert slugify(\"Hello, World!\") == \"hello-world\"\n    assert slugify(\"  spaces   everywhere  \") == \"spaces-everywhere\"\n\n\ndef test_case_folding():\n    assert slugify(\"MiXeD CaSe TEXT\") == \"mixed-case-text\"\n\n\ndef test_accents():\n    assert slugify(\"café naïve\") == \"cafe-naive\"\n\n\ndef test_numbers_and_dashes():\n    assert slugify(\"Release v2.0 -- final\") == \"release-v2-0-final\"\n\n\ndef test_empty():\n    assert slugify(\"\") == \"\"\n\n\ndef test_type_error():\n    with pytest.raises(TypeError):\n        slugify(123)"}}