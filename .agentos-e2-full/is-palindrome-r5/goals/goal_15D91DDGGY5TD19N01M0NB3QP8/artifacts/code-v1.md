{"files":{"palindrome.py":"\"\"\"Palindrome checker (case-insensitive).\"\"\"\n\ndef is_palindrome(s) -> bool:\n    \"\"\"Return True if `s` is a palindrome, ignoring case.\n\n    Comparison ignores case; all characters are considered as-is.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"is_palindrome() argument must be a string\")\n    t = s.lower()\n    return t == t[::-1]\n\n\nif __name__ == \"__main__\":\n    # Minimal self-tests\n    cases = [\n        (\"racecar\", True),\n        (\"RaceCar\", True),\n        (\"AbBa\", True),\n        (\"hello\", False),\n        (\"\", True),\n        (\"a\", True),\n        (\"Abc\", False),\n    ]\n    for text, expected in cases:\n        result = is_palindrome(text)\n        assert result == expected, f\"is_palindrome({text!r}) = {result}, expected {expected}\"\n    try:\n        is_palindrome(123)\n    except TypeError:\n        pass\n    else:\n        raise AssertionError(\"non-string input should raise TypeError\")\n    print(\"All tests passed.\")","test_palindrome.py":"import pytest\n\nfrom palindrome import is_palindrome\n\n\nclass TestIsPalindrome:\n    def test_simple_palindrome(self):\n        assert is_palindrome(\"racecar\") is True\n\n    def test_case_insensitive(self):\n        assert is_palindrome(\"RaceCar\") is True\n\n    def test_mixed_case(self):\n        assert is_palindrome(\"AbBa\") is True\n\n    def test_not_palindrome(self):\n        assert is_palindrome(\"hello\") is False\n\n    def test_empty_string(self):\n        assert is_palindrome(\"\") is True\n\n    def test_single_char(self):\n        assert is_palindrome(\"x\") is True\n\n    def test_case_only_difference(self):\n        assert is_palindrome(\"Aa\") is True\n\n    def test_non_palindrome_with_caps(self):\n        assert is_palindrome(\"Python\") is False\n\n    def test_non_string_raises(self):\n        with pytest.raises(TypeError):\n            is_palindrome(123)\n\n    def test_return_type_is_bool(self):\n        assert isinstance(is_palindrome(\"abba\"), bool)\n        assert isinstance(is_palindrome(\"abc\"), bool)"}}