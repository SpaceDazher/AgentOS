{"files":{"slug.py":"\"\"\"slugify: convert a string to a URL-friendly slug.\"\"\"\n\nimport re\nimport unicodedata\n\n\ndef slugify(s: str) -> str:\n    \"\"\"Return a lowercase, hyphen-separated slug from *s*.\n\n    'Hello World' -> 'hello-world'\n    \"\"\"\n    # Normalize unicode and strip accents (é -> e).\n    s = unicodedata.normalize(\"NFKD\", s)\n    s = \"\".join(ch for ch in s if not unicodedata.combining(ch))\n    # Lowercase, replace any run of non-alphanumeric chars with a hyphen.\n    s = re.sub(r\"[^a-z0-9]+\", \"-\", s.lower())\n    # Trim leading/trailing hyphens.\n    return s.strip(\"-\")","test_slug.py":"import pytest\n\nfrom slug import slugify\n\n\ndef test_basic():\n    assert slugify(\"Hello World\") == \"hello-world\"\n\n\ndef test_multiple_spaces():\n    assert slugify(\"Hello   World\") == \"hello-world\"\n\n\ndef test_punctuation():\n    assert slugify(\"Hello, World!\") == \"hello-world\"\n\n\ndef test_mixed_case_and_numbers():\n    assert slugify(\"Version 2 Release\") == \"version-2-release\"\n\n\ndef test_trim_hyphens():\n    assert slugify(\"  --Hello World--  \") == \"hello-world\"\n\n\ndef test_accents():\n    assert slugify(\"Café Crème\") == \"cafe-creme\"\n\n\ndef test_empty():\n    assert slugify(\"\") == \"\"\n\n\ndef test_returns_str():\n    assert isinstance(slugify(\"Hello World\"), str)"}}