{"files":{"dictutil.py":"\"\"\"Deep merge utility without mutating inputs.\"\"\"\n\n\ndef merge(a: dict, b: dict) -> dict:\n    \"\"\"Return a new dict that is a deep merge of ``a`` and ``b``.\n\n    Neither ``a`` nor ``b`` (nor any nested dicts within them) is mutated.\n    Values from ``b`` take precedence on conflict; nested dicts are merged\n    recursively; any other value types are replaced by ``b``'s value.\n    \"\"\"\n    result = dict(a)\n    for key, b_val in b.items():\n        a_val = result.get(key)\n        if isinstance(a_val, dict) and isinstance(b_val, dict):\n            result[key] = merge(a_val, b_val)\n        else:\n            result[key] = b_val\n    return result","test_dictutil.py":"import copy\n\nfrom dictutil import merge\n\n\ndef test_shallow_merge():\n    assert merge({\"a\": 1}, {\"b\": 2}) == {\"a\": 1, \"b\": 2}\n\n\ndef test_b_precedence():\n    assert merge({\"a\": 1}, {\"a\": 2}) == {\"a\": 2}\n\n\ndef test_nested_merge():\n    a = {\"x\": {\"p\": 1, \"q\": 2}}\n    b = {\"x\": {\"q\": 3, \"r\": 4}}\n    assert merge(a, b) == {\"x\": {\"p\": 1, \"q\": 3, \"r\": 4}}\n\n\ndef test_no_mutation():\n    a = {\"x\": {\"p\": 1}}\n    b = {\"x\": {\"q\": 2}, \"y\": 5}\n    a_copy = copy.deepcopy(a)\n    b_copy = copy.deepcopy(b)\n    result = merge(a, b)\n    assert a == a_copy and b == b_copy\n    # result must not alias input sub-dicts either\n    result[\"x\"][\"p\"] = 999\n    assert a[\"x\"][\"p\"] == 1\n\n\ndef test_empty():\n    assert merge({}, {}) == {}\n    assert merge({\"a\": 1}, {}) == {\"a\": 1}\n    assert merge({}, {\"b\": 2}) == {\"b\": 2}\n\n\ndef test_returns_dict():\n    assert isinstance(merge({}, {}), dict)\n\n\nif __name__ == \"__main__\":\n    for name in sorted(k for k in dir() if k.startswith(\"test_\")):\n        globals()[name]()\n        print(f\"{name} OK\")\n    print(\"All tests passed.\")"}}