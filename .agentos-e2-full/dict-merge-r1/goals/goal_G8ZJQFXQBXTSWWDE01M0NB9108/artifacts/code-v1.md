{"files":{"dictutil.py":"\"\"\"dictutil: non-mutating deep merge.\"\"\"\n\ndef merge(a, b):\n    \"\"\"Deep-merge dicts a and b; returns a new dict without mutating inputs.\n\n    - Values present only in one side are taken as-is.\n    - When both values are dicts, they are merged recursively.\n    - Otherwise b's value wins.\n    Non-dict inputs are accepted at the top level like any leaf value.\n    \"\"\"\n    if isinstance(a, dict) and isinstance(b, dict):\n        result = dict(a)  # shallow copy; nested levels rebuilt below\n        for key, b_val in b.items():\n            if key in result:\n                result[key] = merge(result[key], b_val)\n            else:\n                result[key] = b_val\n        return result\n    return b\n\n\n# --- tests ---\ndef _run_tests():\n    # 1. basic override\n    assert merge({\"x\": 1}, {\"x\": 2}) == {\"x\": 2}\n    # 2. union of keys\n    assert merge({\"a\": 1}, {\"b\": 2}) == {\"a\": 1, \"b\": 2}\n    # 3. nested merge\n    assert merge({\"a\": {\"n\": 1}}, {\"a\": {\"m\": 2}}) == {\"a\": {\"n\": 1, \"m\": 2}}\n    # 4. no mutation of inputs\n    a = {\"a\": {\"n\": 1}}\n    b = {\"a\": {\"m\": 2}}\n    snapshot_a = repr(a)\n    snapshot_b = repr(b)\n    r = merge(a, b)\n    assert repr(a) == snapshot_a and repr(b) == snapshot_b\n    assert r is not a and r[\"a\"] is not a[\"a\"]\n    # 5. b's non-dict wins over a's dict\n    assert merge({\"a\": {\"n\": 1}}, {\"a\": 5}) == {\"a\": 5}\n    # 6. empty dicts\n    assert merge({}, {}) == {}\n    assert merge({\"x\": 1}, {}) == {\"x\": 1}\n    # 7. deep nesting\n    r = merge({\"l1\": {\"l2\": {\"v\": 1}}}, {\"l1\": {\"l2\": {\"w\": 2}}})\n    assert r == {\"l1\": {\"l2\": {\"v\": 1, \"w\": 2}}}\n    print(\"all dictutil.merge tests passed\")\n\n\nif __name__ == \"__main__\":\n    _run_tests()"}}