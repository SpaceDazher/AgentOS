{"files":{"palindrome.py":"\"\"\"Palindrome checker.\n\nis_palindrome(s) -> bool: True if `s` reads the same forwards and\nbackwards, ignoring case. Comparison is case-insensitive; all other\ncharacters (spaces, punctuation, digits) are significant.\n\"\"\"\n\n\ndef is_palindrome(s: str) -> bool:\n    if not isinstance(s, str):\n        raise TypeError(\"is_palindrome expects a string\")\n    normalized = s.lower()\n    return normalized == normalized[::-1]","test_palindrome.py":"import pytest\n\nfrom palindrome import is_palindrome\n\n\nclass TestIsPalindrome:\n    def test_simple_palindrome(self):\n        assert is_palindrome(\"racecar\") is True\n\n    def test_not_palindrome(self):\n        assert is_palindrome(\"hello\") is False\n\n    def test_case_insensitive(self):\n        # Definition of done: comparison ignores case.\n        assert is_palindrome(\"RaceCar\") is True\n        assert is_palindrome(\"Racecar\") is True\n        assert is_palindrome(\"RACECAR\") is True\n\n    def test_mixed_case_not_palindrome(self):\n        # Case-folding must not mask a real mismatch.\n        assert is_palindrome(\"Race\") is False\n        assert is_palindrome(\"AbCa\") is False\n\n    def test_single_character(self):\n        assert is_palindrome(\"a\") is True\n        assert is_palindrome(\"Z\") is True\n\n    def test_empty_string(self):\n        assert is_palindrome(\"\") is True\n\n    def test_two_characters(self):\n        assert is_palindrome(\"aa\") is True\n        assert is_palindrome(\"ab\") is False\n\n    def test_with_spaces_and_punctuation_significant(self):\n        # Only case is ignored; other chars matter.\n        assert is_palindrome(\"nurses run\") is False\n        assert is_palindrome(\"a, a\") is False\n\n    def test_unicode_case_folding(self):\n        assert is_palindrome(\"Шалаш\") is True\n        assert is_palindrome(\"Ärger\".lower() == \"regreä\" and False or False) is False  # sanity no-op\n        assert is_palindrome(\"Öffo\".lower()) == is_palindrome(\"öffo\")\n\n    def test_returns_bool(self):\n        result = is_palindrome(\"abba\")\n        assert isinstance(result, bool)\n\n    def test_non_string_raises(self):\n        with pytest.raises(TypeError):\n            is_palindrome(12321)"}}