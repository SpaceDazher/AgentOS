{"files":{"clamp.py":"\"\"\"clamp(v, lo, hi): constrain v to the inclusive range [lo, hi].\"\"\"\n\n\ndef clamp(v, lo, hi):\n    \"\"\"Clamp v into the inclusive range [lo, hi].\n\n    >>> clamp(5, 0, 10)\n    5\n    >>> clamp(-3, 0, 10)\n    0\n    >>> clamp(42, 0, 10)\n    10\n    \"\"\"\n    if lo > hi:\n        raise ValueError(\"lo must be <= hi\")\n    return max(lo, min(v, hi))","test_clamp.py":"import pytest\n\nfrom clamp import clamp\n\n\ndef test_value_within_range_unchanged():\n    assert clamp(5, 0, 10) == 5\n\n\ndef test_value_below_lo_clamped():\n    assert clamp(-3, 0, 10) == 0\n\n\ndef test_value_above_hi_clamped():\n    assert clamp(42, 0, 10) == 10\n\n\ndef test_boundaries_inclusive():\n    assert clamp(0, 0, 10) == 0\n    assert clamp(10, 0, 10) == 10\n\n\ndef test_negative_range():\n    assert clamp(-7, -5, -1) == -5\n\n\ndef test_floats():\n    assert clamp(1.5, 0.0, 1.0) == 1.0\n    assert isinstance(clamp(1.5, 0.0, 2.0), float)\n\n\ndef test_inverted_bounds_raise():\n    with pytest.raises(ValueError):\n        clamp(5, 10, 0)"}}