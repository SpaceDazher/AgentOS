{"files":{"flatten.py":"\"\"\"flatten_once: flatten a list of lists exactly one level.\"\"\"\n\nfrom typing import Any, Iterable, List\n\n\ndef flatten_once(xss: Iterable[Iterable[Any]]) -> List[Any]:\n    \"\"\"Flatten one level of nesting.\n\n    >>> flatten_once([[1, 2], [3]])\n    [1, 2, 3]\n    >>> flatten_once([[1, [2]], [3]])\n    [1, [2], 3]\n    >>> flatten_once([])\n    []\n    \"\"\"\n    return [item for xs in xss for item in xs]\n\n\ndef test_basic() -> None:\n    assert flatten_once([[1, 2], [3]]) == [1, 2, 3]\n\ndef test_single_level_only() -> None:\n    assert flatten_once([[1, [2]], [3]]) == [1, [2], 3]\n\ndef test_empty() -> None:\n    assert flatten_once([]) == []\n    assert flatten_once([[], []]) == []\n\ndef test_strings_and_tuples() -> None:\n    assert flatten_once([(\"a\", \"b\"), [\"c\"]]) == [\"a\", \"b\", \"c\"]\n    assert flatten_once([[0]]) == [0]\n\nif __name__ == \"__main__\":\n    test_basic()\n    test_single_level_only()\n    test_empty()\n    test_strings_and_tuples()\n    print(\"all tests passed\")"}}