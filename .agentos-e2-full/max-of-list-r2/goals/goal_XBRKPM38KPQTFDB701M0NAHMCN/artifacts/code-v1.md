{"files":{"lists.py":"\"\"\"Module lists.py — maximum(xs) -> number.\"\"\"\n\ndef maximum(xs):\n    \"\"\"Return the largest element of xs.\n\n    Raises ValueError if xs is empty.\n    \"\"\"\n    if not xs:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    it = iter(xs)\n    result = next(it)\n    for x in it:\n        if x > result:\n            result = x\n    return result","test_lists.py":"\"\"\"Tests for lists.maximum.\"\"\"\nimport math\nimport pytest\n\nfrom lists import maximum\n\n\ndef test_maximum_basic_ints():\n    assert maximum([1, 2, 3]) == 3\n    assert maximum([3, 1, 2]) == 3\n    assert maximum([7]) == 7\n\n\ndef test_maximum_negative_numbers():\n    assert maximum([-5, -1, -3]) == -1\n    assert maximum([-42]) == -42\n\n\ndef test_maximum_floats():\n    assert maximum([1.5, 2.75, 0.25]) == 2.75\n\n\ndef test_maximum_mixed_numeric():\n    assert maximum([1, 2.5, -3]) == 2.5\n\n\ndef test_maximum_duplicates():\n    assert maximum([4, 4, 4]) == 4\n\n\ndef test_maximum_empty_raises():\n    with pytest.raises(ValueError):\n        maximum([])\n\n\ndef test_returns_number_type():\n    result = maximum([1, 9, 2])\n    assert isinstance(result, (int, float))\n    assert not isinstance(result, bool)\n    assert math.isfinite(maximum([0.0, float(\"inf\")]))"}}