{"files":{"divmod2.py":"def _divmod(a, b):\n    if b == 0:\n        raise ZeroDivisionError(\"division by zero\")\n    q = a // b\n    r = a % b\n    return q, r\n\n\ndef div_check(a, b):\n    \"\"\"Return [q, r] where a = q * b + r (floor division semantics).\"\"\"\n    try:\n        q, r = _divmod(a, b)\n    except TypeError as exc:\n        raise TypeError(f\"div_check requires numeric arguments: {exc}\") from exc\n    return [q, r]","test_divmod2.py":"import math\n\nimport pytest\n\nfrom divmod2 import div_check\n\n\nclass TestDivCheckBasic:\n    def test_exact_division(self):\n        assert div_check(10, 2) == [5, 0]\n\n    def test_with_remainder(self):\n        assert div_check(7, 3) == [2, 1]\n\n    def test_identity_holds(self):\n        for a in range(-20, 21):\n            for b in range(-5, 6):\n                if b == 0:\n                    continue\n                q, r = div_check(a, b)\n                assert q * b + r == a\n\n    def test_remainder_sign_matches_python(self):\n        # Python floor-division: remainder has sign of divisor.\n        assert div_check(7, -3) == [-3, -2]\n        assert div_check(-7, 3) == [-3, 2]\n\n    def test_float_inputs(self):\n        q, r = div_check(7.5, 2)\n        assert q * 2 + r == 7.5\n        assert isinstance(q, float)\n\n    def test_returns_list(self):\n        result = div_check(9, 4)\n        assert isinstance(result, list)\n        assert len(result) == 2\n\n\nclass TestDivCheckErrors:\n    def test_zero_divisor_raises(self):\n        with pytest.raises(ZeroDivisionError):\n            div_check(1, 0)\n\n    def test_non_numeric_raises_type_error(self):\n        with pytest.raises(TypeError):\n            div_check(\"a\", 2)\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(pytest.main([__file__, \"-v\"]))"}}