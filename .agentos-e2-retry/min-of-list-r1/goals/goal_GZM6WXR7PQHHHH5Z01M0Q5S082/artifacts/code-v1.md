{"files":{"lists2.py":"def minimum(xs):\n    \"\"\"Return the smallest number in xs. Raises ValueError on empty input.\"\"\"\n    if not xs:\n        raise ValueError(\"minimum() arg is an empty sequence\")\n    return min(xs)","test_lists2.py":"import pytest\n\nfrom lists2 import minimum\n\n\ndef test_minimum_basic():\n    assert minimum([3, 1, 2]) == 1\n\n\ndef test_minimum_single_element():\n    assert minimum([42]) == 42\n\n\ndef test_minimum_negatives():\n    assert minimum([-5, -1, -10]) == -10\n\n\ndef test_minimum_floats():\n    assert minimum([2.5, 2.4, 2.6]) == 2.4\n\n\ndef test_minimum_empty_raises():\n    with pytest.raises(ValueError):\n        minimum([])\n\n\ndef test_minimum_returns_number():\n    assert isinstance(minimum([1, 2, 3]), (int, float))"}}