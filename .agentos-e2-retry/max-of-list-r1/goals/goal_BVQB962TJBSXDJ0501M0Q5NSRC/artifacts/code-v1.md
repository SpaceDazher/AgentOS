{"files":{"lists.py":"def maximum(xs):\n    \"\"\"Return the largest number in xs. Raises ValueError on empty input.\"\"\"\n    if not xs:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    best = xs[0]\n    for x in xs[1:]:\n        if x > best:\n            best = x\n    return best","test_lists.py":"import math\n\nimport pytest\n\nfrom lists import maximum\n\n\ndef test_basic_integers():\n    assert maximum([1, 2, 3]) == 3\n\n\ndef test_negative_numbers():\n    assert maximum([-5, -1, -10]) == -1\n\n\ndef test_single_element():\n    assert maximum([42]) == 42\n\n\ndef test_floats():\n    assert maximum([1.5, 2.5, 0.0]) == 2.5\n\n\ndef test_mixed_int_float():\n    assert maximum([1, 2.5, 2]) == 2.5\n\n\ndef test_duplicates():\n    assert maximum([7, 7, 7]) == 7\n\n\ndef test_empty_raises():\n    with pytest.raises(ValueError):\n        maximum([])\n\n\ndef test_returns_element_from_input():\n    xs = [3.14]\n    result = maximum(xs)\n    assert result == 3.14\n    assert isinstance(result, float)"}}