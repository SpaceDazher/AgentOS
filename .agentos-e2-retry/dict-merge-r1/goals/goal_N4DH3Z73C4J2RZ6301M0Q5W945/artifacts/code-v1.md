{"files":{"dictutil.py":"\"\"\"Recursive dict merge utilities.\"\"\"\n\n\ndef merge(a, b):\n    \"\"\"Return a new dict that is a deep merge of a and b.\n\n    Neither input is mutated. Values from b take precedence;\n    nested dicts are merged recursively.\n    \"\"\"\n    if not isinstance(a, dict) or not isinstance(b, dict):\n        raise TypeError(\"merge() expects two dicts\")\n\n    result = {}\n    for key in set(a) | set(b):\n        if key in a and key in b:\n            av, bv = a[key], b[key]\n            if isinstance(av, dict) and isinstance(bv, dict):\n                result[key] = merge(av, bv)\n            else:\n                result[key] = bv\n        elif key in a:\n            result[key] = _copy(a[key])\n        else:\n            result[key] = _copy(b[key])\n    return result\n\n\ndef _copy(value):\n    \"\"\"Best-effort defensive copy of non-merged values.\"\"\"\n    if isinstance(value, dict):\n        return {k: _copy(v) for k, v in value.items()}\n    if isinstance(value, list):\n        return [_copy(v) for v in value]\n    return value","test_dictutil.py":"import unittest\n\nfrom dictutil import merge\n\n\nclass TestMerge(unittest.TestCase):\n    def test_flat_merge_b_wins(self):\n        self.assertEqual(merge({\"x\": 1}, {\"y\": 2}), {\"x\": 1, \"y\": 2})\n        self.assertEqual(merge({\"x\": 1}, {\"x\": 2}), {\"x\": 2})\n\n    def test_no_mutation(self):\n        a = {\"x\": 1}\n        b = {\"y\": 2}\n        snapshot_a = {\"x\": 1}\n        snapshot_b = {\"y\": 2}\n        merge(a, b)\n        self.assertEqual(a, snapshot_a)\n        self.assertEqual(b, snapshot_b)\n\n    def test_nested_recursive(self):\n        a = {\"cfg\": {\"p\": 1, \"q\": 2}}\n        b = {\"cfg\": {\"q\": 3, \"r\": 4}}\n        self.assertEqual(merge(a, b), {\"cfg\": {\"p\": 1, \"q\": 3, \"r\": 4}})\n\n    def test_nested_no_mutation(self):\n        a = {\"cfg\": {\"p\": 1}}\n        b = {\"cfg\": {\"q\": 2}}\n        merge(a, b)\n        self.assertEqual(a[\"cfg\"], {\"p\": 1})\n        self.assertEqual(b[\"cfg\"], {\"q\": 2})\n\n    def test_result_independent_of_inputs(self):\n        a = {\"inner\": {\"k\": 1}}\n        merged = merge(a, {})\n        merged[\"inner\"][\"k\"] = 99\n        self.assertEqual(a[\"inner\"][\"k\"], 1)\n\n    def test_type_mismatch_replaced_not_merged(self):\n        a = {\"x\": {\"deep\": 1}}\n        b = {\"x\": [1, 2]}\n        self.assertEqual(merge(a, b), {\"x\": [1, 2]})\n\n    def test_non_dict_raises(self):\n        with self.assertRaises(TypeError):\n            merge({}, None)\n\n\nif __name__ == \"__main__\":\n    unittest.main()"}}