{"files":{"lists.py":"def maximum(xs):\n    \"\"\"Return the maximum value of a non-empty iterable of numbers.\n\n    Raises:\n        ValueError: if xs is empty.\n    \"\"\"\n    xs = list(xs)\n    if not xs:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    best = xs[0]\n    for x in xs[1:]:\n        if x > best:\n            best = x\n    return best","test_lists.py":"import pytest\n\nfrom lists import maximum\n\n\ndef test_maximum_returns_largest():\n    assert maximum([1, 5, 3]) == 5\n    assert maximum([-10, -3, -50]) == -3\n    assert maximum([7]) == 7\n    assert maximum([2.5, 2.75, 2.0]) == 2.75\n    assert maximum(range(0, 100)) == 99\n\n\ndef test_maximum_empty_raises():\n    with pytest.raises(ValueError):\n        maximum([])\n    with pytest.raises(ValueError):\n        maximum(iter(()))"}}