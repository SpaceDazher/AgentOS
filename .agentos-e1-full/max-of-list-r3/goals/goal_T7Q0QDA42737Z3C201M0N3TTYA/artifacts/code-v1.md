{"files":{"lists.py":"def maximum(xs):\n    \"\"\"Return the maximum value in xs. Raises ValueError on empty input.\"\"\"\n    if not xs:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    best = xs[0]\n    for x in xs[1:]:\n        if x > best:\n            best = x\n    return best","test_lists.py":"import pytest\n\nfrom lists import maximum\n\n\ndef test_maximum_basic():\n    assert maximum([1, 2, 3]) == 3\n    assert maximum([3, 2, 1]) == 3\n    assert maximum([5]) == 5\n\n\ndef test_maximum_negative_and_floats():\n    assert maximum([-10, -3, -7]) == -3\n    assert maximum([1.5, 1.75, 1.25]) == 1.75\n\n\ndef test_maximum_duplicates():\n    assert maximum([4, 4, 4]) == 4\n\n\ndef test_maximum_empty_raises():\n    with pytest.raises(ValueError):\n        maximum([])\n\n\ndef test_returns_element_not_index():\n    xs = [\"apple\", \"zebra\", \"mango\"]\n    assert maximum(xs) == \"zebra\"\n    assert maximum(xs) == max(xs)"}}