{"files":{"lists.py":"\"\"\"Module lists.py — exposes maximum(xs) -> number.\n\nRaises ValueError on an empty sequence.\n\"\"\"\n\n\ndef maximum(xs):\n    \"\"\"Return the maximum element of a non-empty sequence `xs`.\n\n    >>> maximum([3, 1, 2])\n    3\n    >>> maximum([-5])\n    -5\n    >>> maximum([2.5, 7, 7.1])\n    7.1\n\n    >>> maximum([])\n    Traceback (most recent call last):\n        ...\n    ValueError: maximum() arg is an empty sequence\n    \"\"\"\n    if xs is None:\n        raise TypeError(\"maximum() arg is None, expected a non-empty sequence\")\n    if len(xs) == 0:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    it = iter(xs)\n    best = next(it)\n    for x in it:\n        if x > best:\n            best = x\n    return best","test_lists.py":"\"\"\"Tests for lists.maximum.\"\"\"\nimport math\nimport pytest\n\nfrom lists import maximum\n\n\ndef test_int_list():\n    assert maximum([3, 1, 2]) == 3\n    assert maximum([1, 2, 3, 4, 5]) == 5\n    assert maximum([5, 4, 3, 2, 1]) == 5\n\n\ndef test_single_element():\n    assert maximum([42]) == 42\n\n\ndef test_negative_numbers():\n    assert maximum([-5, -1, -3]) == -1\n    assert maximum([-7]) == -7\n\n\ndef test_floats():\n    assert math.isclose(maximum([2.5, 7.0, 7.1]), 7.1)\n    assert maximum([1, 2.5]) == 2.5\n\n\ndef test_duplicates():\n    assert maximum([4, 4, 4]) == 4\n\n\ndef test_mixed_int_float():\n    assert maximum([-1.5, 0, 0.5]) == 0.5\n\n\ndef test_tuple_and_range_inputs():\n    assert maximum((1, 9, 3)) == 9\n    assert maximum(range(10)) == 9\n\n\ndef test_empty_raises_value_error():\n    with pytest.raises(ValueError):\n        maximum([])\n\n\ndef test_none_raises_type_error():\n    with pytest.raises(TypeError):\n        maximum(None)\n\n\ndef test_result_is_element_not_copy():\n    xs = [object(), object()]\n    assert maximum(xs) is xs[1]"}}