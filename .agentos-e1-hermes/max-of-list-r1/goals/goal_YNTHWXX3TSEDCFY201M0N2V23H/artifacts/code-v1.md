{"files":{"lists.py":"def maximum(xs):\n    \"\"\"Return the maximum value of a non-empty iterable of numbers.\n\n    Raises:\n        ValueError: if xs is empty.\n    \"\"\"\n    xs = list(xs)\n    if not xs:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    best = xs[0]\n    for x in xs[1:]:\n        if x > best:\n            best = x\n    return best","test_lists.py":"import math\n\nimport pytest\n\nfrom lists import maximum\n\n\ndef test_maximum_basic_ints():\n    assert maximum([3, 1, 4, 1, 5]) == 5\n\n\ndef test_maximum_single_element():\n    assert maximum([42]) == 42\n\n\ndef test_maximum_negatives():\n    assert maximum([-5, -2, -9]) == -2\n\n\ndef test_maximum_floats():\n    assert maximum([1.5, 2.7, 0.3]) == pytest.approx(2.7)\n\n\ndef test_maximum_mixed_int_float():\n    assert maximum([1, 2.5, 2]) == 2.5\n\n\ndef test_maximum_duplicates():\n    assert maximum([7, 7, 7]) == 7\n\n\ndef test_maximum_empty_raises():\n    with pytest.raises(ValueError):\n        maximum([])\n\n\ndef test_maximum_matches_builtin():\n    data = [10, -3, 8, 8.25, 0]\n    assert maximum(data) == max(data)\n\n\ndef test_maximum_nan_propagates_like_builtin():\n    data = [1.0, float(\"nan\"), 3.0]\n    result = maximum(data)\n    assert math.isnan(result) or result == 3.0"}}