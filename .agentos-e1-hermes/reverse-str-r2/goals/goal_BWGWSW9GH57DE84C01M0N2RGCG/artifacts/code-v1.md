{"files":{"strutil.py":"\"\"\"strutil — string utilities.\"\"\"\n\ndef reverse(s: str) -> str:\n    \"\"\"Return the reverse of the input string.\n\n    Args:\n        s: The string to reverse. Must be a str (or support slicing).\n    Returns:\n        The reversed string.\n    Raises:\n        TypeError: If s is not a str.\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(f\"reverse() expects str, got {type(s).__name__}\")\n    return s[::-1]","test_strutil.py":"\"\"\"Tests for strutil.reverse.\"\"\"\nimport unittest\n\nfrom strutil import reverse\n\n\nclass TestReverse(unittest.TestCase):\n    def test_basic(self):\n        self.assertEqual(reverse(\"hello\"), \"olleh\")\n\n    def test_empty(self):\n        self.assertEqual(reverse(\"\"), \"\")\n\n    def test_single_char(self):\n        self.assertEqual(reverse(\"a\"), \"a\")\n\n    def test_palindrome(self):\n        self.assertEqual(reverse(\"racecar\"), \"racecar\")\n\n    def test_spaces_and_punctuation(self):\n        self.assertEqual(reverse(\"abc def!\"), \"!fed cba\")\n\n    def test_unicode(self):\n        self.assertEqual(reverse(\"привет\"), \"тевирп\")\n        self.assertEqual(reverse(\"héllo\"), \"olléh\")\n\n    def test_involution(self):\n        s = \"round trip\"\n        self.assertEqual(reverse(reverse(s)), s)\n\n    def test_type_error_on_non_str(self):\n        for bad in (123, None, [\"a\"], b\"bytes\"):\n            with self.assertRaises(TypeError):\n                reverse(bad)\n\n\nif __name__ == \"__main__\":\n    unittest.main()"}}