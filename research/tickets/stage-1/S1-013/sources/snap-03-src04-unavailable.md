# Source-resolution record: SRC-04 UNAVAILABLE

- sought: SRC-04, `40_mental_model.md`, §7–8 (internal source of the
  five comprehension questions QM2 per the ticket).
- searched: this checkout (`research/`), the S1-011 worktree
  (`D:/Project/AgentOS/.codex-work/s1-011-task/research`) and the
  S1-012 worktree (`.../s1-012-task/research`) — file absent
  everywhere (read-only directory listings, 2026-09-04).
- status: **unavailable**. No result is attributed to SRC-04.
- substitution (explicit, not equivalent): the five measures C1–C5
  are grounded operationally in the frozen pilot protocol
  (pilot-protocol.json + rubric.json), whose transfer tasks require
  applying each concept to a novel principal/scope scenario. The
  substitution is recorded as a limitation: mental-model coverage
  claims rest on the task text and the protocol, not on SRC-04 §7–8.
- re-entry: if SRC-04 becomes available, re-resolve through the
  source registry, re-freeze the protocol as a new version, and
  re-run the dry-run battery before any human phase.
