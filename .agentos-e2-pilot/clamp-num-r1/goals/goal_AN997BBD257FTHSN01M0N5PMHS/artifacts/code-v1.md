{"files":{"clamp.py":"def clamp(v, lo, hi):\n    \"\"\"Clamp v into the inclusive range [lo, hi].\n\n    >>> clamp(5, 0, 10)\n    5\n    >>> clamp(-1, 0, 10)\n    0\n    >>> clamp(11, 0, 10)\n    10\n    \"\"\"\n    return max(lo, min(v, hi))","test_clamp.py":"from clamp import clamp\n\n\ndef test_within_range():\n    assert clamp(5, 0, 10) == 5\n    assert clamp(2.5, 0, 10) == 2.5\n\n\ndef test_below_low():\n    assert clamp(-1, 0, 10) == 0\n    assert clamp(-100.5, -50.5, 50.5) == -50.5\n\n\ndef test_above_high():\n    assert clamp(11, 0, 10) == 10\n    assert clamp(99.9, 0, 50.25) == 50.25\n\n\ndef test_boundaries_inclusive():\n    assert clamp(0, 0, 10) == 0\n    assert clamp(10, 0, 10) == 10\n\n\ndef test_negative_range():\n    assert clamp(-5, -10, -1) == -5\n    assert clamp(-15, -10, -1) == -10"}}