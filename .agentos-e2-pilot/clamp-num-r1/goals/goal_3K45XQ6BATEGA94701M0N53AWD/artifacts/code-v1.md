{"files":{"clamp.py":"\"\"\"Clamp a value into [lo, hi].\"\"\"\n\n\ndef clamp(v, lo, hi):\n    \"\"\"Return v clamped to the range [lo, hi].\n\n    >>> clamp(5, 0, 10)\n    5\n    >>> clamp(-3, 0, 10)\n    0\n    >>> clamp(42, 0, 10)\n    10\n    >>> clamp(2.5, 1.5, 3.5)\n    2.5\n    >>> clamp(1.0, 1.5, 3.5)\n    1.5\n    >>> clamp(4.0, 1.5, 3.5)\n    3.5\n    \"\"\"\n    if lo > hi:\n        raise ValueError(\"lo must be <= hi\")\n    if v < lo:\n        return lo\n    if v > hi:\n        return hi\n    return v\n\n\nif __name__ == \"__main__\":\n    import doctest\n    failures, _ = doctest.testmod()\n    assert failures == 0, \"doctests failed\"\n    # additional assertions\n    assert clamp(0, -1, 1) == 0\n    assert clamp(-100, -50, 50) == -50\n    assert clamp(100, -50, 50) == 50\n    try:\n        clamp(0, 5, 1)\n        raise AssertionError(\"expected ValueError\")\n    except ValueError:\n        pass\n    print(\"all tests passed\")"}}