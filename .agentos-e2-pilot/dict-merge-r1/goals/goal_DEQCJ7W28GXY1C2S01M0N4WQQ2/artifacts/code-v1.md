{"files":{"dictutil.py":"\"\"\"dictutil: merge(a, b) -> dict without mutating inputs.\"\"\"\n\n\ndef merge(a: dict, b: dict) -> dict:\n    \"\"\"Return a new dict with keys of `a` overridden by keys of `b`.\n\n    Neither input is mutated; nested dicts are shallow-copied into the result.\n    \"\"\"\n    if not isinstance(a, dict) or not isinstance(b, dict):\n        raise TypeError(\"merge() expects two dicts\")\n    result = dict(a)\n    result.update(b)\n    return result","test_dictutil.py":"import copy\n\nimport pytest\n\nfrom dictutil import merge\n\n\ndef test_merge_basic():\n    assert merge({\"a\": 1}, {\"b\": 2}) == {\"a\": 1, \"b\": 2}\n\n\ndef test_merge_override():\n    assert merge({\"a\": 1}, {\"a\": 2}) == {\"a\": 2}\n\n\ndef test_no_mutation_of_inputs():\n    a = {\"x\": 1}\n    b = {\"y\": 2}\n    a_copy = copy.deepcopy(a)\n    b_copy = copy.deepcopy(b)\n    result = merge(a, b)\n    assert a == a_copy and b == b_copy\n    assert result is not a and result is not b\n\n\ndef test_result_is_dict():\n    assert isinstance(merge({}, {}), dict)\n\n\ndef test_empty():\n    assert merge({}, {\"k\": 1}) == {\"k\": 1}\n    assert merge({\"k\": 1}, {}) == {\"k\": 1}\n\n\ndef test_non_dict_raises():\n    with pytest.raises(TypeError):\n        merge([1], {})\n    with pytest.raises(TypeError):\n        merge({}, None)"}}