{"files":{"lists2.py":"\"\"\"lists2 — minimum(xs) with ValueError on empty input.\"\"\"\n\n\ndef minimum(xs):\n    \"\"\"Return the smallest element of *xs*.\n\n    Raises:\n        ValueError: if xs is empty.\n    \"\"\"\n    iterator = iter(xs)\n    try:\n        current = next(iterator)\n    except StopIteration:\n        raise ValueError(\"minimum() arg is an empty sequence\")\n    for x in iterator:\n        if x < current:\n            current = x\n    return current","test_lists2.py":"import pytest\n\nfrom lists2 import minimum\n\n\nclass TestMinimum:\n    def test_basic_ints(self):\n        assert minimum([3, 1, 2]) == 1\n\n    def test_single_element(self):\n        assert minimum([42]) == 42\n\n    def test_negative_numbers(self):\n        assert minimum([-5, -1, -10]) == -10\n\n    def test_duplicates(self):\n        assert minimum([7, 7, 7]) == 7\n\n    def test_floats(self):\n        assert minimum([2.5, 1.5, 3.0]) == 1.5\n\n    def test_mixed_int_float(self):\n        assert minimum([1, 0.5, 2]) == 0.5\n\n    def test_tuple_input(self):\n        assert minimum((9, 4, 6)) == 4\n\n    def test_generator_input(self):\n        assert minimum(x * 2 for x in range(5)) == 0\n\n    def test_empty_raises_value_error(self):\n        with pytest.raises(ValueError):\n            minimum([])\n\n    def test_empty_generator_raises_value_error(self):\n        with pytest.raises(ValueError):\n            minimum(iter([]))\n\n    def test_error_message_mentions_empty(self):\n        with pytest.raises(ValueError, match=\"empty\"):\n            minimum([])"}}