{"files":{"slug.py":"import re\nimport unicodedata\n\n\ndef slugify(s: str) -> str:\n    \"\"\"Convert a string to a URL-friendly slug.\n\n    Transliterates non-ASCII characters, collapses runs of\n    non-alphanumeric characters into single hyphens, and lowercases.\n    'Hello World' -> 'hello-world'.\n    \"\"\"\n    s = unicodedata.normalize(\"NFKD\", str(s)).encode(\"ascii\", \"ignore\").decode(\"ascii\")\n    s = re.sub(r\"[^a-zA-Z0-9]+\", \"-\", s)\n    return s.strip(\"-\").lower()","test_slug.py":"import pytest\n\nfrom slug import slugify\n\n\ndef test_basic():\n    assert slugify(\"Hello World\") == \"hello-world\"\n\n\ndef test_multiple_spaces_and_punctuation():\n    assert slugify(\"  Hello,   World!  \") == \"hello-world\"\n\n\ndef test_already_slug():\n    assert slugify(\"hello-world\") == \"hello-world\"\n\n\ndef test_unicode_transliteration():\n    assert slugify(\"Héllo Wörld\") == \"hello-world\"\n\n\ndef test_empty_string():\n    assert slugify(\"\") == \"\"\n\n\ndef test_non_alnum_only():\n    assert slugify(\"--- !!! ---\") == \"\"\n\n\ndef test_returns_str():\n    assert isinstance(slugify(123), str)"}}