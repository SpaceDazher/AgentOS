# Byte-stable evidence must stay byte-identical across OS checkouts:
# the CI SHA-256 pin over the research evidence review fails on Windows
# when autocrlf rewrites LF -> CRLF at checkout time.
research/** -text
