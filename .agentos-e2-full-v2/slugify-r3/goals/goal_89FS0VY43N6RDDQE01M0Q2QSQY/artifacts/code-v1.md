{"files":{"slug.py":"import re\nimport unicodedata\n\n\ndef slugify(s: str) -> str:\n    \"\"\"Convert a string to a URL-friendly slug.\n\n    'Hello World' -> 'hello-world'\n    \"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"slugify expects a string\")\n    # Normalize unicode and strip diacritics\n    text = unicodedata.normalize(\"NFKD\", s)\n    text = \"\".join(ch for ch in text if not unicodedata.combining(ch))\n    # Lowercase\n    text = text.lower()\n    # Replace any run of non-alphanumeric chars with a hyphen\n    text = re.sub(r\"[^a-z0-9]+\", \"-\", text)\n    # Trim leading/trailing hyphens\n    return text.strip(\"-\")","test_slug.py":"from slug import slugify\n\n\ndef test_basic():\n    assert slugify('Hello World') == 'hello-world'\n\n\ndef test_multiple_spaces_and_punct():\n    assert slugify('  Hello,   World!  ') == 'hello-world'\n\n\ndef test_already_slug():\n    assert slugify('hello-world') == 'hello-world'\n\n\ndef test_unicode():\n    assert slugify('Héllo Wörld') == 'hello-world'\n\n\ndef test_numbers():\n    assert slugify('Release 2.0 Beta') == 'release-2-0-beta'\n\n\nif __name__ == '__main__':\n    test_basic()\n    test_multiple_spaces_and_punct()\n    test_already_slug()\n    test_unicode()\n    test_numbers()\n    print('all tests passed')"}}