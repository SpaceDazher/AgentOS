{"files":{"test_vowels.py":"import pytest\nfrom vowels import count_vowels\n\n\ndef test_empty_string():\n    assert count_vowels(\"\") == 0\n\n\ndef test_no_vowels():\n    assert count_vowels(\"bcdfg\") == 0\n    assert count_vowels(\"xyz\") == 0\n\n\ndef test_all_vowels_lowercase():\n    assert count_vowels(\"aeiou\") == 5\n\n\ndef test_uppercase():\n    assert count_vowels(\"AEIOU\") == 5\n\n\ndef test_mixed_case_word():\n    assert count_vowels(\"Hello World\") == 3\n\n\ndef test_counts_each_occurrence():\n    assert count_vowels(\"aaa\") == 3\n\n\ndef test_digits_and_punctuation_ignored():\n    assert count_vowels(\"a1b2c3!e?\") == 2\n\n\ndef test_unicode_cyrillic_not_counted():\n    # Latin vowels only; Cyrillic vowels are not counted\n    assert count_vowels(\"мама\") == 0\n\n\ndef test_non_string_raises():\n    with pytest.raises(TypeError):\n        count_vowels(123)","vowels.py":"def count_vowels(s: str) -> int:\n    \"\"\"Return the number of vowels (a, e, i, o, u) in s, case-insensitive.\"\"\"\n    if not isinstance(s, str):\n        raise TypeError(\"s must be a string\")\n    return sum(1 for ch in s if ch.lower() in \"aeiou\")"}}