{"files":{"clamp.py":"def clamp(v, lo, hi):\n    \"\"\"Clamp v into the inclusive range [lo, hi].\"\"\"\n    return max(lo, min(v, hi))","test_clamp.py":"from clamp import clamp\n\n\ndef test_within_range():\n    assert clamp(5, 0, 10) == 5\n\n\ndef test_below_low():\n    assert clamp(-3, 0, 10) == 0\n\n\ndef test_above_high():\n    assert clamp(42, 0, 10) == 10\n\n\ndef test_boundaries_inclusive():\n    assert clamp(0, 0, 10) == 0\n    assert clamp(10, 0, 10) == 10\n\n\ndef test_floats():\n    assert clamp(3.5, 1.0, 2.0) == 2.0\n\n\ndef test_lo_greater_than_hi_returns_lo():\n    # max(lo, min(v, hi)) degenerates to lo when lo > hi\n    assert clamp(5, 10, 0) == 10"}}