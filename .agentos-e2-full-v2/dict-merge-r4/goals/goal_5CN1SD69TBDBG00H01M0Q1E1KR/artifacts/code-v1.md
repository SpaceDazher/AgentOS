{"files":{"dictutil.py":"\"\"\"dictutil: non-mutating dict merge.\"\"\"\n\n\ndef merge(a, b):\n    \"\"\"Merge dicts a and b into a new dict without mutating inputs.\n\n    Values from b override values from a on key collisions.\n    Nested dicts are merged recursively (also without mutation).\n    \"\"\"\n    result = dict(a)  # shallow copy of a; a is never touched\n    for key, value in b.items():\n        if (\n            key in result\n            and isinstance(result[key], dict)\n            and isinstance(value, dict)\n        ):\n            result[key] = merge(result[key], value)\n        else:\n            result[key] = value\n    return result\n\n\n# --- tests ---\nif __name__ == \"__main__\":\n    # flat override\n    assert merge({\"x\": 1}, {\"y\": 2}) == {\"x\": 1, \"y\": 2}\n    # collision: b wins\n    assert merge({\"x\": 1}, {\"x\": 9}) == {\"x\": 9}\n    # nested recursive merge\n    assert merge({\"n\": {\"a\": 1, \"b\": 2}}, {\"n\": {\"b\": 3, \"c\": 4}}) == {\n        \"n\": {\"a\": 1, \"b\": 3, \"c\": 4}\n    }\n    # inputs are not mutated\n    a = {\"x\": 1, \"n\": {\"a\": 1}}\n    b = {\"y\": 2, \"n\": {\"b\": 2}}\n    snapshot_a = {\"x\": 1, \"n\": {\"a\": 1}}\n    snapshot_b = {\"y\": 2, \"n\": {\"b\": 2}}\n    merged = merge(a, b)\n    assert a == snapshot_a and b == snapshot_b\n    assert merged == {\"x\": 1, \"y\": 2, \"n\": {\"a\": 1, \"b\": 2}}\n    # empty inputs\n    assert merge({}, {}) == {}\n    assert isinstance(merge({}, {}), dict)\n    print(\"all tests passed\")"}}