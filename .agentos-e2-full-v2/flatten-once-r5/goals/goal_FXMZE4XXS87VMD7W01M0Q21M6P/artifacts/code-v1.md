{"files":{"flatten.py":"\"\"\"flatten_once(xss): flatten a list of lists by exactly one level.\n\n>>> flatten_once([[1, 2], [3]])\n[1, 2, 3]\n\"\"\"\nfrom __future__ import annotations\n\n\ndef flatten_once(xss) -> list:\n    \"\"\"Return a new list with the elements of each inner sequence of xss,\n    flattened by exactly one level. Non-list items at the top level are\n    not recursed into beyond one level.\n    \"\"\"\n    result = []\n    for item in xss:\n        try:\n            iterator = iter(item)\n        except TypeError:\n            result.append(item)\n            continue\n        # Strings/bytes would explode into chars; treat them as atoms.\n        if isinstance(item, (str, bytes)):\n            result.append(item)\n        else:\n            result.extend(iterator)\n    return result\n\n\nif __name__ == \"__main__\":\n    tests = [\n        ((( [1, 2], [3] ),), [1, 2, 3]),\n        (([],), []),\n        ((([1], [], [2, 3]),), [1, 2, 3]),\n        ((((1, 2), [3]),), [1, 2, 3]),\n        (([[[1]], [[2]]],), [[1], [2]]),\n        (([\"ab\", [\"cd\"]],), [\"ab\", \"cd\"]),\n        (((1, [2]),), [1, 2]),\n    ]\n    for args, expected in tests:\n        got = flatten_once(*args)\n        assert got == expected, f\"flatten_once{args!r} -> {got!r}, expected {expected!r}\"\n    print(\"all tests passed\")"}}