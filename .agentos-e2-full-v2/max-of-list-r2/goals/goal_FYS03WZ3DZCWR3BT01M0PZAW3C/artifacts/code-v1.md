{"files":{"lists.py":"def maximum(xs):\n    \"\"\"Return the maximum value of a non-empty sequence of numbers.\n\n    Raises:\n        ValueError: if xs is empty.\n    \"\"\"\n    if not xs:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    result = xs[0]\n    for x in xs[1:]:\n        if x > result:\n            result = x\n    return result","test_lists.py":"import math\n\nimport pytest\n\nfrom lists import maximum\n\n\ndef test_returns_max_of_positive_ints():\n    assert maximum([1, 3, 2]) == 3\n\n\ndef test_returns_max_of_negative_numbers():\n    assert maximum([-5, -1, -3]) == -1\n\n\ndef test_single_element():\n    assert maximum([42]) == 42\n\n\ndef test_floats_and_ints_mixed():\n    assert maximum([1, 2.5, 2]) == 2.5\n\n\ndef test_duplicates():\n    assert maximum([7, 7, 7]) == 7\n\n\ndef test_works_on_tuple():\n    assert maximum((4, 9, 0)) == 9\n\n\ndef test_empty_raises_value_error():\n    with pytest.raises(ValueError):\n        maximum([])\n\n\ndef test_nan_propagates():\n    result = maximum([1.0, float(\"nan\")])\n    assert math.isnan(result)"}}