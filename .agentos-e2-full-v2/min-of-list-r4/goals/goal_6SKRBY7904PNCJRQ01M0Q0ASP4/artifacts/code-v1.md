{"files":{"lists2.py":"def minimum(xs):\n    \"\"\"Return the smallest element of a non-empty sequence.\n\n    Raises ValueError if xs is empty.\n    \"\"\"\n    if not xs:\n        raise ValueError(\"minimum() arg is an empty sequence\")\n    result = xs[0]\n    for x in xs[1:]:\n        if x < result:\n            result = x\n    return result","test_lists2.py":"import math\n\nimport pytest\n\nfrom lists2 import minimum\n\n\ndef test_minimum_basic_ints():\n    assert minimum([3, 1, 2]) == 1\n\n\ndef test_minimum_single_element():\n    assert minimum([42]) == 42\n\n\ndef test_minimum_negative_numbers():\n    assert minimum([-5, -2, -10]) == -10\n\n\ndef test_minimum_floats():\n    assert minimum([2.5, 1.25, 3.0]) == 1.25\n\n\ndef test_minimum_empty_raises():\n    with pytest.raises(ValueError):\n        minimum([])\n\n\ndef test_minimum_mixed_int_float():\n    assert minimum([2, 1.5, 3]) == 1.5\n\n\ndef test_minimum_returns_actual_element():\n    xs = [7, 4, 9]\n    m = minimum(xs)\n    assert m in xs\n    assert all(m <= x for x in xs)\n\n\ndef test_minimum_matches_builtin_min_random():\n    import random\n\n    random.seed(1234)\n    for _ in range(100):\n        xs = [random.randint(-1000, 1000) for _ in range(random.randint(1, 50))]\n        assert minimum(xs) == min(xs)"}}