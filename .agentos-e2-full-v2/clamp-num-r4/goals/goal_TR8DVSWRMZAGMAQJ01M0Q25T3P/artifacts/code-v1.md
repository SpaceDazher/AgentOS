{"files":{"clamp.py":"\"\"\"Clamp a value into a closed range [lo, hi].\"\"\"\n\n\ndef clamp(v, lo, hi):\n    \"\"\"Return v constrained to the closed interval [lo, hi].\n\n    >>> clamp(5, 0, 10)\n    5\n    >>> clamp(-3, 0, 10)\n    0\n    >>> clamp(42, 0, 10)\n    10\n    >>> clamp(2.5, 0, 2)\n    2\n    \"\"\"\n    if lo > hi:\n        raise ValueError(\"lo must be <= hi\")\n    return max(lo, min(v, hi))","test_clamp.py":"import pytest\n\nfrom clamp import clamp\n\n\ndef test_clamp_value_inside_range():\n    assert clamp(5, 0, 10) == 5\n\n\ndef test_clamp_value_below_lo():\n    assert clamp(-3, 0, 10) == 0\n\n\ndef test_clamp_value_above_hi():\n    assert clamp(42, 0, 10) == 10\n\n\ndef test_clamp_at_boundaries():\n    assert clamp(0, 0, 10) == 0\n    assert clamp(10, 0, 10) == 10\n\n\ndef test_clamp_floats():\n    assert clamp(1.5, 0.0, 1.0) == 1.0\n    assert clamp(0.25, 0.0, 1.0) == 0.25\n\n\ndef test_clamp_negative_range():\n    assert clamp(0, -10, -1) == -1\n\n\ndef test_clamp_inverted_range_raises():\n    with pytest.raises(ValueError):\n        clamp(5, 10, 0)"}}