{"files":{"lists2.py":"\"\"\"Minimum of a list.\"\"\"\n\n\ndef minimum(xs):\n    \"\"\"Return the smallest element of xs.\n\n    Raises:\n        ValueError: if xs is empty.\n    \"\"\"\n    if not xs:\n        raise ValueError(\"minimum() arg is an empty sequence\")\n    result = xs[0]\n    for x in xs[1:]:\n        if x < result:\n            result = x\n    return result","test_lists2.py":"import pytest\n\nfrom lists2 import minimum\n\n\ndef test_minimum_basic():\n    assert minimum([3, 1, 2]) == 1\n\n\ndef test_minimum_single():\n    assert minimum([42]) == 42\n\n\ndef test_minimum_negatives():\n    assert minimum([-5, -1, -10]) == -10\n\n\ndef test_minimum_floats():\n    assert minimum([1.5, 0.5, 2.0]) == 0.5\n\n\ndef test_minimum_empty_raises():\n    with pytest.raises(ValueError):\n        minimum([])"}}