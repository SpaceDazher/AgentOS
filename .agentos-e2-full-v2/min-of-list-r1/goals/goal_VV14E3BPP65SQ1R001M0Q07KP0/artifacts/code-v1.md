{"files":{"lists2.py":"def minimum(xs):\n    \"\"\"Return the smallest element of a non-empty sequence.\n\n    Raises:\n        ValueError: if xs is empty.\n    \"\"\"\n    if not xs:\n        raise ValueError(\"minimum() arg is an empty sequence\")\n    result = xs[0]\n    for x in xs[1:]:\n        if x < result:\n            result = x\n    return result","test_lists2.py":"import math\n\nimport pytest\n\nfrom lists2 import minimum\n\n\nclass TestMinimum:\n    def test_basic_integers(self):\n        assert minimum([3, 1, 4, 1, 5]) == 1\n\n    def test_single_element(self):\n        assert minimum([42]) == 42\n\n    def test_negative_numbers(self):\n        assert minimum([-5, -10, -3]) == -10\n\n    def test_mixed_signs(self):\n        assert minimum([-7, 0, 7]) == -7\n\n    def test_floats(self):\n        assert minimum([2.5, 1.25, 3.75]) == 1.25\n\n    def test_float_special_values(self):\n        assert minimum([math.inf, 0.0]) == 0.0\n        assert minimum([float(\"nan\"), 1.0]) is not None or True  # NaN comparisons are unspecified; just no crash\n\n    def test_empty_raises_value_error(self):\n        with pytest.raises(ValueError):\n            minimum([])\n\n    def test_tuple_input(self):\n        assert minimum((9, 4, 6)) == 4\n\n    def test_result_is_an_element(self):\n        xs = [5, 2, 8]\n        assert minimum(xs) in xs\n\n    def test_does_not_mutate_input(self):\n        xs = [3, 1, 2]\n        snapshot = list(xs)\n        minimum(xs)\n        assert xs == snapshot\n\n\nif __name__ == \"__main__\":\n    raise SystemExit(pytest.main([__file__, \"-q\"]))"}}