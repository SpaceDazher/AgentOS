{"files":{"dictutil.py":"\"\"\"dictutil: deep merge without mutating inputs.\"\"\"\n\n\ndef merge(a, b):\n    \"\"\"Return a new dict that is a deep merge of ``a`` and ``b``.\n\n    Values from ``b`` override values from ``a``. When both sides hold\n    dicts under the same key, they are merged recursively. Neither input\n    is mutated.\n    \"\"\"\n    if not isinstance(a, dict) or not isinstance(b, dict):\n        raise TypeError(\"merge() expects two dicts\")\n    result = dict(a)\n    for key, bval in b.items():\n        aval = result.get(key)\n        if isinstance(aval, dict) and isinstance(bval, dict):\n            result[key] = merge(aval, bval)\n        else:\n            result[key] = bval\n    return result","test_dictutil.py":"import copy\n\nfrom dictutil import merge\n\n\ndef test_flat_merge():\n    assert merge({\"a\": 1}, {\"b\": 2}) == {\"a\": 1, \"b\": 2}\n\n\ndef test_override():\n    assert merge({\"a\": 1}, {\"a\": 2}) == {\"a\": 2}\n\n\ndef test_deep_merge():\n    a = {\"x\": {\"y\": 1, \"z\": 2}}\n    b = {\"x\": {\"z\": 3, \"w\": 4}}\n    assert merge(a, b) == {\"x\": {\"y\": 1, \"z\": 3, \"w\": 4}}\n\n\ndef test_no_mutation():\n    a = {\"x\": {\"y\": 1}}\n    b = {\"x\": {\"z\": 2}}\n    a_copy = copy.deepcopy(a)\n    b_copy = copy.deepcopy(b)\n    merge(a, b)\n    assert a == a_copy and b == b_copy\n\n\ndef test_non_dict_value_replaced():\n    assert merge({\"x\": {\"y\": 1}}, {\"x\": 5}) == {\"x\": 5}\n    assert merge({\"x\": 5}, {\"x\": {\"y\": 1}}) == {\"x\": {\"y\": 1}}\n\n\ndef test_empty_inputs():\n    d = {\"k\": 1}\n    assert merge({}, d) == d\n    assert merge(d, {}) == d\n\n\ndef test_type_error():\n    try:\n        merge([1], {\"a\": 1})\n    except TypeError:\n        pass\n    else:\n        raise AssertionError(\"expected TypeError\")"}}