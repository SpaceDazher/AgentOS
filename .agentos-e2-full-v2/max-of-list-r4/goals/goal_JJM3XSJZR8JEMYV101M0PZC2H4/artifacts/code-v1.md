{"files":{"lists.py":"\"\"\"Module lists: maximum(xs) -> number, raises ValueError on empty.\"\"\"\n\n\ndef maximum(xs):\n    \"\"\"Return the maximum value of a non-empty iterable of numbers.\n\n    Raises:\n        ValueError: if xs is empty.\n    \"\"\"\n    xs = list(xs)\n    if not xs:\n        raise ValueError(\"maximum() arg is an empty sequence\")\n    result = xs[0]\n    for x in xs[1:]:\n        if x > result:\n            result = x\n    return result","test_lists.py":"import pytest\n\nfrom lists import maximum\n\n\ndef test_maximum_returns_largest():\n    assert maximum([3, 1, 4, 1, 5]) == 5\n\n\ndef test_maximum_single_element():\n    assert maximum([42]) == 42\n\n\ndef test_maximum_negative_numbers():\n    assert maximum([-5, -2, -9]) == -2\n\n\ndef test_maximum_floats_and_ints_mixed():\n    assert maximum([1.5, 3, 2]) == 3\n\n\ndef test_maximum_accepts_any_iterable():\n    assert maximum(x * 2 for x in range(5)) == 8\n\n\ndef test_maximum_empty_raises_value_error():\n    with pytest.raises(ValueError):\n        maximum([])\n\n\ndef test_maximum_empty_generator_raises_value_error():\n    with pytest.raises(ValueError):\n        maximum(iter([]))"}}